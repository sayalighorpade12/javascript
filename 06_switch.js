







// Marriage eligibility
// dayNumber 1 - Monday
// 2 - Tuesday
// 3 - Wednesday
// 4 - Thursday
// 5 - Friday
// 6 - Saturday
// 7 - Sunday

/*function weekDayName(dayNumber) {
if (dayNumber==1) {
    console.log(`Day is: Monday as day number is ${dayNumber}`);
} else {
    if (dayNumber==2) {
        console.log(`Day is: Tuesday as day number is ${dayNumber}`);
    } else {
        if (dayNumber==3) {
            console.log(`Day is: Wednesday as day number is ${dayNumber}`);
        } else {
            if (dayNumber==4) {
                console.log(`Day is: Thursday as day number is ${dayNumber}`);
            } else {
                if (dayNumber==5) {
                    console.log(`Day is: Friday as day number is ${dayNumber}`);
                } else {
                    if (dayNumber==6) {
                        console.log(`Day is: Saturday as day number is ${dayNumber}`);
                    } else {
                        if (dayNumber==7) {
                            console.log(`Day is: Sunday as day number is ${dayNumber}`);
                        } else {
                            console.log(`Invalid data: ${dayNumber}`);
                        }
                    }
                }
            }
            
        }
    }
}

}
weekDayName(1);
weekDayName(2);
weekDayName(3);
weekDayName(4);
weekDayName(5);
weekDayName(6);
weekDayName(7);*/


console.log("                                                                                                                                     ");


function weekDay(dayNum) {
    switch (dayNum) {
        case 1:
            console.log(`Day is: Monday as day number is ${dayNum}`);
            break;
        case 2:
            console.log(`Day is: Tuesday as day number is ${dayNum}`);    
            break;
        case 3:
            console.log(`Day is: Wednesday as day number is ${dayNum}`);
            break;
        case 4:
            console.log(`Day is: Thursday as day number is ${dayNum}`);
            break;
        case 5:
            console.log(`Day is: Friday as day number is ${dayNum}`);
            break;
        case 6:
            console.log(`Day is: Saturday as day number is ${dayNum}`);
            break;
        case 7:
            console.log(`Day is: Sunday as day number is ${dayNum}`);
            break;
        default:
            console.log(`Invalid input: ${dayNum}`);
            break;
    }
}


weekDay(7);

weekDay(1);

weekDay(2);
weekDay(3);
weekDay(4);
weekDay(5);
weekDay(6);
weekDay(7);
weekDay(8);
weekDay(null);
weekDay(undefined);
weekDay(NaN);
















